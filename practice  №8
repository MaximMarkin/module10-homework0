

let listAuto = new Map();
    listAuto.set("Ford", "Mustang");
    listAuto.set("Dodge", "Challendger");
    listAuto.set("Ferrari", "Tributo");
    listAuto.set("Lamborgini", "Huracan");
    
for (let list of listAuto.keys()) {
    console.log(`Ключ-${list}, Значение-${listAuto.get(list)}`);
}